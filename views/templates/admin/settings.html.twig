{% extends '@PrestaShop/Admin/layout.html.twig' %}

{% block content %}
    {% include '@PrestaShop/Admin/Common/Grid/grid_panel.html.twig' with {'grid': settingsGrid} %}
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script src="{{ asset('themes/default/js/bundle/pagination.js') }}"></script>
    <script>
        $('#sucuri_settings_grid_action_common_refresh_list').click(function (event) {
            event.stopPropagation();
            window.location = '{{ path('admin_sucuri_refresh_settings') }}'
        });
    </script>
{% endblock %}